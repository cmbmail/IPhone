spring:
  application:
    name: phonebiz
  datasource:
    url: jdbc:mysql://localhost:3306/phonebiz?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:Cmb@123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
        type:
          enum:
            strategy: string
        jakarta.persistence.lock.timeout: 5000
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: false
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Shanghai
    property-naming-strategy: SNAKE_CASE
server:
  port: 8080
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

phonebiz:
  jwt:
    secret: ${JWT_SECRET:yG-K6za6s941O78p2sQkeMBWop5Bg6r9hhEwQ8GDljeMQs7v57sWgmIiwePpGF_q}
    expiration: 86400000
  lock:
    max-fail-count: 5
    lock-duration-minutes: 30
