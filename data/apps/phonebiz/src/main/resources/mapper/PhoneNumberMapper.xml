<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmbchina.phonebiz.mapper.PhoneNumberMapper">

    <resultMap id="BaseResultMap" type="com.cmbchina.phonebiz.entity.PhoneNumber">
        <id column="id" property="id"/>
        <result column="phone_number" property="phoneNumber"/>
        <result column="status" property="status"/>
        <result column="employee_id" property="employeeId"/>
        <result column="employee_name" property="employeeName"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM phone_number
        WHERE status = #{status}
        AND del_flag = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByEmployeeId" resultMap="BaseResultMap">
        SELECT * FROM phone_number
        WHERE employee_id = #{employeeId}
        AND del_flag = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByPhoneNumber" resultMap="BaseResultMap">
        SELECT * FROM phone_number
        WHERE phone_number = #{phoneNumber}
        AND del_flag = 0
    </select>

</mapper>
