<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmbchina.phonebiz.mapper.SysEmployeeRoleMapper">

    <insert id="insertBatch">
        INSERT INTO sys_employee_role (employee_id, role_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.employeeId}, #{item.roleId})
        </foreach>
    </insert>

    <delete id="deleteByEmployeeId">
        DELETE FROM sys_employee_role WHERE employee_id = #{employeeId}
    </delete>

    <select id="selectRoleIdsByEmployeeId" resultType="java.lang.Long">
        SELECT role_id FROM sys_employee_role WHERE employee_id = #{employeeId}
    </select>

</mapper>