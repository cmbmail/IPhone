export interface PhoneStatistics {
  totalCount: number
  allocatedCount: number
  idleCount: number
  stoppedCount: number
}

export interface DeviceStatistics {
  totalCount: number
  onlineCount: number
  offlineCount: number
  onlineRate: number
}
