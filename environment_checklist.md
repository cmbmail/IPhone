# PhoneBiz 环境检查清单

> 日期：2026-05-15
> 版本：1.0

---

## 1. 后端环境检查

| 检查项 | 验证命令 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|---------|------|
| Java 17 安装 | `java -version` | OpenJDK 17.x.x | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| JAVA_HOME 配置 | `echo $JAVA_HOME` | 指向 JDK 17 | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| Gradle 8.7 安装 | `gradle --version` | 8.7 | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| MySQL 8 安装 | `mysql --version` | 8.0.x | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| Git 安装 | `git --version` | 任意（推荐 ≥2.x） | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| Docker（可选） | `docker --version` | 20.10+ | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| Docker Compose（可选） | `docker-compose --version` | 2.24+ | ⚠️ 需要运行命令验证 | 🔄 待检查 |

---

## 2. 前端环境检查

| 检查项 | 验证命令 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|---------|------|
| Node.js 20 LTS | `node -v` | v20.x.x | ⚠️ 需要运行命令验证 | 🔄 待检查 |
| npm 安装 | `npm -v` | 9.x+ | ⚠️ 需要运行命令验证 | 🔄 待检查 |

---

## 3. 系统要求

| 环境 | 要求 | 实际值 | 状态 |
|------|------|-------|------|
| 磁盘空间 | 后端 500MB+，前端 2GB+ | ⚠️ 需要检查 | 🔄 待检查 |
| 内存 | 4GB+（推荐 8GB+） | ⚠️ 需要检查 | 🔄 待检查 |

---

## 4. 下一步

完成环境检查后，请：

1. 安装缺失的组件
2. 升级不符合要求的版本
3. 再次运行检查，确保全部通过

---

## 备注

> 所有环境检查完成后，请填写此文档并保存，以便后续团队开发参考。
